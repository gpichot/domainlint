# _artifacts/skill_tree.yaml
# Generated by skill-tree-generator
library:
  name: 'domainlint'
  version: '0.7.0'
  repository: 'https://github.com/gpichot/domainlint'
  description: 'CLI architecture linter for TypeScript/JavaScript that enforces feature boundaries and detects import cycles, with a programmatic Rule API for custom architectural constraints.'
generated_from:
  domain_map: '_artifacts/domain_map.yaml'
  skill_spec: '_artifacts/skill_spec.md'
generated_at: '2026-06-02'

# Flat structure — 5 task-focused skills, no router, no core overview.
# All skills live in packages/domainlint/skills/<slug>/SKILL.md.
# Each is independently discoverable via `npx @tanstack/intent list`.

skills:
  - name: 'adopt-on-existing-codebase'
    slug: 'adopt-on-existing-codebase'
    type: 'lifecycle'
    domain: 'adoption-lifecycle'
    path: 'skills/adopt-on-existing-codebase/SKILL.md'
    package: 'packages/domainlint'
    description: >
      Run domainlint for the first time on an existing TypeScript/JavaScript
      project, triage the Day-1 wave of violations, and decide what to fix,
      warn, or disable per feature. Covers `domainlint check`, the
      `--feature` flag, severity selection, `overrides.features` blocks for
      legacy code, and the install-before-lint requirement for tsconfig
      paths. Load when adopting domainlint, landing it in CI, or recovering
      from "200 violations on day one".
    requires: []
    sources:
      - 'gpichot/domainlint:docs/src/content/docs/getting-started.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/ci.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/configuration.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/cli.mdx'
      - 'gpichot/domainlint:README.md'
    references: []

  - name: 'configure'
    slug: 'configure'
    type: 'core'
    domain: 'configuration'
    path: 'skills/configure/SKILL.md'
    package: 'packages/domainlint'
    description: >
      Author and edit `domainlint.json` to match the project layout, set
      rule severities, and apply per-feature overrides. Covers `srcDir`,
      `featuresDir`, `barrelFiles`, `extensions`, `tsconfigPath`, `exclude`,
      `includeDynamicImports`, severity keys (`import-cycles`,
      `cross-feature-imports`, `no-external-feature-imports`), severity
      values (`error`/`warn`/`off`), the global → per-feature override
      resolution order, and the Zod validation that gates exit code 2.
      Load when shaping the static config surface.
    requires: []
    sources:
      - 'gpichot/domainlint:docs/src/content/docs/configuration.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/rules.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/feature-structure.mdx'
      - 'gpichot/domainlint:packages/domainlint/src/config/'
    references:
      - 'references/config-schema.md'

  - name: 'write-custom-rule'
    slug: 'write-custom-rule'
    type: 'core'
    domain: 'rule-authoring'
    path: 'skills/write-custom-rule/SKILL.md'
    package: 'packages/domainlint'
    description: >
      Author a custom `Rule` in `domainlint.rules.ts` using the
      `GraphQuery` API and `emitViolation` to enforce project-specific
      import constraints — including layered or domain-driven
      architectures. Covers the `Rule` interface (`name` + `check`), the
      `RuleContext` shape (`query`, `graph`, `config`, `emitViolation`),
      the full 11-method `GraphQuery` surface (`edgesFrom`, `edgesTo`,
      `edgesBetween`, `filesMatching`, `featureOf`, `importsOf`,
      `importersOf`, `dependsOn`, `dependenciesOf`, `dependentsOf`,
      `fanOut`, `fanIn`), the `query.edgesFrom(...).violations(code, msg)`
      shorthand, and how to model DDD bounded contexts and layered
      architectures as rules. Load before generating any custom rule code.
    requires: []
    sources:
      - 'gpichot/domainlint:docs/src/content/docs/custom-rules.mdx'
      - 'gpichot/domainlint:docs/src/content/docs/graphquery-api.mdx'
      - 'gpichot/domainlint:packages/domainlint/src/rules/'
      - 'gpichot/domainlint:packages/domainlint/src/graph/graph-query.ts'
    references:
      - 'references/graphquery-api.md'
      - 'references/ddd-rule-cookbook.md'

  - name: 'use-in-monorepo'
    slug: 'use-in-monorepo'
    type: 'core'
    domain: 'workspace'
    path: 'skills/use-in-monorepo/SKILL.md'
    package: 'packages/domainlint'
    description: >
      Run domainlint across npm, pnpm, or yarn workspaces, configure
      cross-package import restrictions via `packageRules`, and write
      workspace-level custom rules using the `WorkspaceRule` interface.
      Covers workspace detection (`pnpm-workspace.yaml`, then
      `package.json` `workspaces`), per-package `domainlint.json` /
      `tsconfig.json`, the silent-skip behavior for packages without
      `srcDir`, the built-in `noPackageImport` / `noPackageCycle` rules,
      the `WorkspaceRuleContext` shape (`packages`, `edges`,
      `packageRules`, `emitViolation`), and `packageRulesFile`. Load when
      working at the workspace root or writing cross-package rules.
    requires: []
    sources:
      - 'gpichot/domainlint:docs/src/content/docs/workspaces.mdx'
      - 'gpichot/domainlint:packages/domainlint/src/workspace/'
      - 'gpichot/domainlint:packages/domainlint/src/rules/workspace-rules.ts'
      - 'gpichot/domainlint:packages/domainlint/src/rules/package-import-deny-rule.ts'
      - 'gpichot/domainlint:packages/domainlint/src/rules/package-cycle-detector.ts'
    references: []

  - name: 'debug-a-violation'
    slug: 'debug-a-violation'
    type: 'core'
    domain: 'debugging'
    path: 'skills/debug-a-violation/SKILL.md'
    package: 'packages/domainlint'
    description: >
      Diagnose why a specific file is being flagged, shrink noisy cycle
      output, and trace the offending import. Covers `domainlint debug
      <file>` for per-file import + violation dumps, `--shortest-cycles`
      to collapse long SCCs to minimum-length cycles,
      `--max-cycle-length=<n>` to suppress cycles longer than n
      (default 50), `--feature=<name>` to scope output, `--verbose` for
      per-package output in workspaces, and reading the
      `a -> b -> c -> a` cycle format. Load when investigating a single
      file or a specific cycle.
    requires: []
    sources:
      - 'gpichot/domainlint:docs/src/content/docs/cli.mdx'
      - 'gpichot/domainlint:packages/domainlint/src/commands/debug.ts'
      - 'gpichot/domainlint:packages/domainlint/src/commands/check.ts'
      - 'gpichot/domainlint:packages/domainlint/src/reporter/'
    references: []

# Notes for generate-skill (Step 3):
#
# - All skills live in packages/domainlint/skills/<slug>/. No router skill,
#   no core overview — agents discover via `npx @tanstack/intent list`.
# - Failure modes per skill come from _artifacts/domain_map.yaml. Cross-skill
#   failure modes (with multiple slugs in `skills:`) must appear in every
#   listed file. The current cross-skill modes are:
#     * "Project layout does not match defaults" → adopt-on-existing-codebase, configure
#     * "Excludes a noisy file via exclude instead of override" → adopt-on-existing-codebase, configure
#     * "Confuses Rule with WorkspaceRule" → write-custom-rule, use-in-monorepo
#     * "Re-implements built-in / override-able check" → write-custom-rule, configure
#     * "Forgets --feature during cleanup" → debug-a-violation, adopt-on-existing-codebase
# - Tensions to surface in each affected skill's Common Mistakes section:
#     * Adoption ease vs strictness (adopt-on-existing-codebase ↔ configure)
#     * Custom rule expressiveness vs config-first simplicity (write-custom-rule ↔ configure)
#     * Module-level Rule vs workspace-level WorkspaceRule (write-custom-rule ↔ use-in-monorepo)
# - Cross-references to add as "See also" links (no duplication of content):
#     * adopt-on-existing-codebase → configure, debug-a-violation
#     * configure → write-custom-rule
#     * write-custom-rule → use-in-monorepo
#     * use-in-monorepo → configure
# - Reference files needed:
#     * write-custom-rule/references/graphquery-api.md — full 11-method API
#       surface with signatures, return types, and minimal examples. #1
#       hallucination surface per maintainer.
#     * write-custom-rule/references/ddd-rule-cookbook.md — worked DDD examples
#       (layered architecture: domain → application → infrastructure; bounded
#       contexts; aggregate boundaries). Maintainer-requested.
#     * configure/references/config-schema.md — full domainlint.json schema
#       reference including the Zod schema and the rule-key vs violation-code
#       table (the #1 CRITICAL failure mode in configure).
# - target library_version in every SKILL.md frontmatter: 0.7.0
